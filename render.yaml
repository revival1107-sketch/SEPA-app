services:
  - type: web
    name: sepa-stock-finder
    env: python
    plan: free
    buildCommand: "pip install -r requirements-web.txt"
    startCommand: "gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
